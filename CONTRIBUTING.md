# 기여 가이드

## 작업 흐름

1. 작업을 시작하기 전에 이슈를 생성하거나 선택합니다.
2. 이슈에서 브랜치를 생성합니다.
3. Conventional Commits 형식으로 커밋합니다.
4. 브랜치를 푸시하고 풀 리퀘스트를 엽니다.
5. 풀 리퀘스트에 관련 이슈를 연결합니다.
6. 필요한 검증을 실행하고 결과를 기록합니다.
7. 머지 전에 풀 리퀘스트를 리뷰합니다.
8. 레포지토리에서 승인된 머지 방식으로 머지합니다.

## 브랜치 네이밍

다음 형식을 사용합니다:

```txt
<type>/<issue-number>-<short-description>
```

예시:

```txt
feat/123-login-page
fix/124-token-refresh
chore/125-project-setup
```

## 커밋 메시지

Conventional Commits 형식을 사용합니다.

기본 형식:

```txt
<type>: <summary>
```

본문이 필요한 경우 제목과 본문 사이에 빈 줄을 둡니다.

```txt
<type>: <summary>

- 주요 변경 사항
- 검증 또는 참고 사항
```

커밋 본문은 다음 경우에 사용합니다.

- 변경 범위가 여러 파일, 화면, 모듈에 걸치는 경우
- 커밋 제목만으로 변경 의도나 맥락을 설명하기 어려운 경우
- 리뷰어가 확인해야 할 주요 변경 사항이 여러 개인 경우
- 후속 작업, 제외 범위, 검증 결과를 함께 남겨야 하는 경우

작은 변경은 제목만 사용해도 됩니다.

예시:

```txt
feat: 로그인 화면 추가
fix: 토큰 갱신 오류 수정
chore: 프로젝트 초기 설정
```

본문을 포함한 예시:

```txt
feat: 시스템 설정 화면 퍼블리싱

- 시스템 설정 통계 카드 추가
- 설정 항목 테이블 추가
- 저장 액션 toast 처리
```

## 이슈

- 이슈는 조직 공통 이슈 템플릿을 사용합니다.
- 작업 범위는 구현해야 할 핵심 작업을 일반적인 문장으로 작성합니다.
- 완료 기준은 체크박스 목록으로 작성합니다.
- 완료 기준은 구현 여부를 판단할 수 있는 확인 가능한 문장으로 작성합니다.
- 파일명이나 내부 구현 세부사항만으로 완료 기준을 작성하지 않습니다.

예시:

```md
## 완료 기준

- [ ] 관리자가 설정 화면에 접근할 수 있다.
- [ ] 주요 통계 정보를 확인할 수 있다.
- [ ] 저장 버튼을 눌렀을 때 저장 액션을 확인할 수 있다.
- [ ] `npm run check`가 통과한다.
```

## 풀 리퀘스트

- `main` 브랜치에 직접 푸시하지 않습니다.
- 하나의 풀 리퀘스트는 하나의 이슈 또는 하나의 논리적 변경에 집중합니다.
- 풀 리퀘스트 제목은 Conventional Commits 형식을 사용합니다.
- 변경 사항 요약을 포함합니다.
- 검증 결과를 포함합니다.
- 해당되는 경우 `Closes #<이슈번호>`로 관련 이슈를 연결합니다.

풀 리퀘스트 제목 형식:

```txt
<type>: <summary>
```

예시:

```txt
feat: 시스템 설정 화면 퍼블리싱
fix: 토큰 갱신 오류 수정
chore: 프로젝트 기본 설정 정리
```

## 검증

리뷰를 요청하기 전에 관련 검증을 실행합니다.

프론트엔드 예시:

```bash
npm run typecheck
npm run lint
npm run check
```

백엔드 레포지토리는 자체적으로 동등한 명령어를 문서화해야 합니다.
